name: Intro to github action

# les "déclencheurs"
on:
    push:

jobs:
    hello-world:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - run: echo "Hello world"